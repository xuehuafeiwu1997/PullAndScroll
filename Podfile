# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Pull And Scroll' do
  # Comment the next line if you don't want to use dynamic frameworks
#  use_frameworks!
   pod 'Masonry'
   pod 'ReactiveObjC'

  # Pods for Pull And Scroll

  target 'Pull And ScrollTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Pull And ScrollUITests' do
    # Pods for testing

  end

end
